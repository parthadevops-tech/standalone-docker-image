import { AppGoWildDirective } from './app-go-wild.directive';

describe('AppGoWildDirective', () => {
  it('should create an instance', () => {
    const directive = new AppGoWildDirective();
    expect(directive).toBeTruthy();
  });
});
